[project]
name = "pitwall-telemetry-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tejasdu", email = "tejasdu@umich.edu" }
]
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
]

[project.scripts]
pitwall-telemetry-engine = "pitwall_telemetry_engine:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
